version: '3.8'
services:
  web:
    build: .
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=sqlite:////app/instance/app.db
      - SECRET_KEY=change-me
    volumes:
      - ./instance:/app/instance
